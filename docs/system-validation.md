# 系统验证记录

验证日期：2026-09-08。

**最新状态：真实模型已在 uv 后端完成接入与六类图片测试。** 详见 [真实模型验证报告](real-model-validation.md)。下方早期模型验证记录保留用于追溯。

## 模拟与业务流程

基础业务测试无需 Torch 或 Ultralytics。现有 9 项 pytest 测试全部通过，覆盖单图双模型推理、坐标及类别校验、阈值过滤、空结果、JSON/PNG 导出、历史快照、重启恢复、文件引用清理、图片方向校正、格式与大小限制、20 个未结束任务上限、失败隔离、单模型缓存切换、OpenAPI 类型，以及框架导入失败后的清理。

前端 TypeScript 生产构建通过，2 项 jsdom 组件交互测试通过，验证上传、双模型提交、检测框显隐、空结果、导出链接、模型目录、历史回看与保存任务恢复。安装依赖的 npm 审计结果为零漏洞。构建有单个 JS 包超过 500 KB 的体积提示，当前演示版未按页面拆包。

另外，通过实际运行的 Vite 代理访问 uv FastAPI，完成了图片上传、双模拟模型推理、JSON/PNG 下载和删除清理的 HTTP 全流程验证。当前会话的浏览器连接工具报告没有可用浏览器，因此没有完成真实浏览器截图或人工视觉验收，不能将组件测试视作截图验收。

## 早期真实模型验证记录（已由上方最新报告更新）

使用已有 Conda 环境 `D:/ruanjian/envs/yolov8/python.exe`，调用新系统的 **同一个 `YoloAdapter`**，只做 CPU 推理，没有修改该环境或训练代码。

- 权重：`runs/exp0_baseline/weights/best.pt`。
- 图片：`pcb-defect-dataset/test/images/light_04_mouse_bite_03_1_600.jpg`。
- 输入尺寸：600 × 600；置信度阈值：0.25。
- 结果：权重加载和推理成功，返回 `detections: []`。

这证明当前权重可通过适配器加载并完成真实推理，**不证明检测准确率达标**。该样例未检出缺陷，需在训练完成后使用固定 checkpoint 及标注测试集进一步评估。

上述早期记录仅验证了旧环境。当前已安装 YOLO extra、解决 Torch DLL 兼容问题，并在 Python 3.11 uv 环境中完成真实模型的 HTTP 推理与导出验证，默认模型目录同时包含一个真实模型和两个模拟模型。
